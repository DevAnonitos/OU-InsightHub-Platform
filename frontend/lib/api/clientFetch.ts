import { setAuthCookies } from "../actions/cookies";

export const clientFetch = async (endpointUrl: string, options: RequestInit = { }) => {
  try {
    
  } catch (error: any) {
    console.log(error.message);
  }
};