import { Request, Response } from "express";
import { responseHandler } from "@/handlers/response.handler";


export const getAllComments = async (req: Request, ) => {
  try {
    
  } catch (error) {
    
  }
};

