import { generateToken, verifyToken } from "@/libs/utils/token.utils";

class TokenService {
    createToken () {

    }

    verifyToken () {
        
    }

    refreshToken () {
        
    }
};

export default new TokenService();